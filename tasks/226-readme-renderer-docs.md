# Task 226 — README: document the full feature surface (docs-only)

**Status:** planned · **Impact:** ⚪ low (docs, high leverage) · **Origin:** task 192 §6

## Problem

README names ~6 of the **18** render engines (`engine-registry.ts:61-241` is the source of
truth) — half of the plugin's strongest feature is invisible to a user/marketplace visitor.
Newer capabilities (persistent render cache, live re-theme, mermaid/echarts/d2 palette
settings, wiki layer, callouts, outline tree, status-bar counters, image pipeline) are
similarly under-documented.

## Scope

- [ ] Renderers section: table of all 18 engines (name, fence lang, notable capabilities —
      d2 `|md|` labels, plantuml offline TeaVM, geojson basemaps note + the
      allowRemoteImages gating), generated BY HAND but cross-checked against
      — and (added 2026-07-03, persona audit) call out the PM/dev-relevant **mermaid 11.15
      diagram TYPES** explicitly: gantt, timeline, kanban (static), user journey, quadrant,
      pie, sankey — verified bundled; nobody discovers these from "mermaid" alone —
      `engine-registry.ts` (add a comment in the registry pointing back so future engines
      update the README — or a small doc-sync test asserting every registry lang appears
      in README; lean the test, it's ~10 lines).
- [ ] Sections: theming (content/code/mermaid/echarts/d2 settings incl. `auto` pairing),
      wiki links (chips/hint/create/ambiguity), callouts, outline (panel + tree), image
      paste pipeline (folder/webp/quality/maxWidth/allowRemoteImages), keyboard shortcuts
      (Ctrl+Alt+E, Ctrl+F — extend as 216/196 land), settings reference table.
- [ ] Keep the CHANGELOG style contract (memory `[[changelog-style-vs-original]]`): README
      describes the product; changelog stays the fork-vs-upstream delta — don't duplicate.

## Out of scope

- Screenshots/GIF overhaul (separate pass), marketplace gallery config, docs site.

## Verification

- The doc-sync test (registry langs ⊆ README) green; `npm run lint:ci` (markdown untouched
  by biome but the test file counts); user review of the rendered README — docs land only
  after their read-through.
