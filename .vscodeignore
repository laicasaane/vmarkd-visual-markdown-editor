.vscode
.github
.azure
media-src
node_modules
src
test
coverage
DEVELOPMENT.md
.gitignore
.env
.env.*
tsconfig.json
build.mjs
package-lock.json
scripts
media/vditor/dist/js/mathjax
# Build-analysis metadata and highlight themes not exposed by vmde.theme.code.
media/dist/main.meta.json
media/vditor/dist/js/highlight.js/styles/base16

# Development-agent content — never ship in the .vsix
AGENTS.md
CLAUDE.md
LOCAL_AGENT_TASK.md
.claude
# Development-agent skills and working state — vsce would otherwise package them.
.agents
.superpowers
.codebase-memory
docs
tasks
test-results
.playwright-cli
.vscode-test
tmp
# Git worktrees (a full repo copy each) — gitignored, but vsce does not read .gitignore, so it would
# otherwise walk into them and hang/bloat the package. Never ship a worktree in the .vsix.
.worktrees
.nvmrc
out

# Development tooling config and agent skill metadata — not needed at runtime
biome.jsonc
knip.jsonc
.jscpd.json
.dependency-cruiser.cjs
.git-blame-ignore-revs
skills-lock.json

# Source maps — debug artifacts, not needed at runtime
**/*.map
