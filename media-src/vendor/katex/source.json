{
    "version": "0.16.9",
    "description": "KaTeX — math typesetting (MIT). Bytes ship inside the Vditor package (dist/js/katex/), so nothing is pinned or copied here; this dir exists only to carry the license text next to those bytes (syncVditorAssets wipes media/vditor/dist on every build, so a hand-placed license file there would not survive).",
    "origin": "https://github.com/KaTeX/KaTeX"
}
