# No D2 here

Just prose and a plain fenced code block — no diagrams at all, so the code-split
`d2-main.js` bundle (task 165) must never be fetched for this document.

Some **bold** and _italic_ prose, a [link](https://example.com), and a list:

- one
- two
- three

```js
const x = 1
console.log(x)
```

That is all.
