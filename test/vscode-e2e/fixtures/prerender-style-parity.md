# Prerender parity heading

A paragraph with `inline code` and a [link](https://example.test).

- First list item
- Second list item

> A quoted paragraph for the prerender-to-live handoff.

| Head | Value |
| --- | --- |
| one | two |

```ts
const prerender = 'must keep its geometry';
```
